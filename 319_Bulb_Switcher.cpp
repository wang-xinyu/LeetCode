/**
 * Bulb Switcher
 * 
 */
class Solution {
public:
    int bulbSwitch(int n) {
        //bulb i ends up on if and only if it has an odd number of divisors.
        // only the square number has odd number of divisors.
        return sqrt(n);
    }
};
